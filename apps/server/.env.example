# Database Configuration (Matches docker-compose.yml defaults)
DATABASE_URL=postgres://postgres:postgrespassword@localhost:5432/kas_gunungsari

# Better Auth Configuration
# Replace this with a strong random string in production
BETTER_AUTH_SECRET=kas_gunungsari_super_secret_key_change_in_prod
BETTER_AUTH_URL=http://localhost:3000/api/auth

# Server Configuration
PORT=3000
